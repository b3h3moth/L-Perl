#!/usr/bin/env perl
use warnings;
use strict;

#The default (scalar) variable is '$_'

#smallest echo program :-)
while (<>) {
    #each line is assigned to $_
    print;

    #print "$_";
    #It's the same thing
}
