#!/usr/bin/env perl
use warnings;
use strict;
use v5.14;

#The regexp quote qr// does exactly that: qr/string/ compiles the string as a
#regexp and transforms the result into a form that can be assigned to a
#variable.
