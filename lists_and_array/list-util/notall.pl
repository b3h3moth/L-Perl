#!/usr/bin/env perl
use warnings;
use strict;
use v5.14;
use List::Util qw(notall);

# notall { BLOCK } LIST;
# It returns true only if no value in the LIST causes the BLOCK to return true, 
# false otherwise.

