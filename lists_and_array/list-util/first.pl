#!/usr/bin/env perl
use warnings;
use strict;
use v5.14;
use List::Util qw(first);

# first { BLOCK } LIST
# 
# It returns  the first element of LIST that satisfies the condition specified
# in the BLOCK.
