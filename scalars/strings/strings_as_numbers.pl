#!/usr/bin/env perl
use warnings;
use strict;
use v5.14;

# Strings used as numbers
my $result = '12' / 3;
say $result;
# Compare string length
my $str1 = 'string1';
my $str2 = 'str2';
if ($str1 gt $str2) {
    say 'ok';
}
